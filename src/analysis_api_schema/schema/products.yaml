id: https://w3id.org/MONet/analysis-api-schema/enums
name: analysis-api-schema-enums
title: MONet Analysis API - Enumerations
description: |-
  Enumerated values used throughout the MONet Analysis API schema
license: MIT

prefixes:
  analysis_api_schema: https://w3id.org/MONet/analysis-api-schema/
  linkml: https://w3id.org/linkml/
  schema: http://schema.org/
default_prefix: analysis_api_schema

imports:
  - linkml:types
  - enums
  - analysis_api_schema

slots:
  measure_type:
    range: productmeasuretype
  rep:
    range: numeric
  aq:
    range: numeric

classes:

  BulkDensityProduct:
    slots:
      - measure_type
    attributes:
      id:
        identifier: true
        range: processedData
      bulk_density_id:
        range: quantityValue
      flag:
        range: processeddataflag

  ElementalAnalysisProduct:
    slots:
      - measure_type
    attributes:
      id:
        identifier: true
        range: processedData
      total_carbon_id:
        range: quantityValue
      total_nitrogen_id:
        range: quantityValue
      total_kjeldahl_nitrogen_id:
        range: quantityValue
      total_sulfur_id:
        range: quantityValue
      flag_total_carbon:
        range: processeddataflag
      flag_total_nitrogen:
        range: processeddataflag
      flag_tkn:
        range: processeddataflag
      flag_total_sulfur:
        range: processeddataflag

  EnzymeProduct:
    slots:
      - measure_type
    attributes:
      id:
        identifier: true
        range: processedData
      beta_glucosidase_ug_pnp_per_g_per_h_id:
        range: quantityValue
      flag:
        range: processeddataflag

  FTICRProduct:
    slots:
      - id
      - measure_type
      - rep
      - aq
    attributes:
      id:
        identifier: true
        range: processedData
      h_c_average:
        range: double
      o_c_average:
        range: double
      c_average:
        range: double
      percent_mz_assigned_id:
        range: quantityValue
      rms_id:
        range: quantityValue
      dbe_average:
        range: double
      low_mass_accuracy_flag:
        range: boolean # TODO should be processeddataflag
      low_mz_assignment_flag:
        range: boolean # TODO should be processeddataflag

  GWCMoistureProduct:
    slots:
      - measure_type
    attributes:
      id:
        identifier: true
        range: processedData
      gwc_percent_id:
        range: quantityValue
      flag:
        range: processeddataflag

  
  HydraulicPropertiesProduct:
    description: >-
      Soil hydraulic parameters derived from HYPROP evaporation-experiment data. One row per core section; the four
      attributes are the four VGM model parameters.  Proposal_ID, sampling_set,
      and core_section are inherited from the parent processedData record.
    slots:
      - measure_type
    attributes:
      id:
        identifier: true
        range: processedData
      alpha:
        description: >-
          Van Genuchten shape parameter α (1/cm). Controls the inverse of the
          air-entry suction; typically fitted by HYPROP-FIT or similar software.
        range: double
      n:
        description: >-
          Van Genuchten pore-size distribution index n (dimensionless, n > 1).
          Controls the slope of the water-retention curve.
        range: double
      theta_r:
        description: >-
          Residual volumetric water content θ_r (cm³ cm⁻³). The water content
          at which liquid conductivity approaches zero.
        range: double
      theta_s:
        description: >-
          Saturated volumetric water content θ_s (cm³ cm⁻³). Approximates
          total porosity under saturated conditions.
        range: double
      flag:
        description: QC flag for the entire VGM fit (e.g. missing sample, failed QC).
        range: processeddataflag
      
  IonsAnalysisProduct:
    slots:
      - measure_type
    attributes:
      id:
        identifier: true
        range: processedData
      sulfate_id:
        range: quantityValue
      boron_id:
        range: quantityValue
      zinc_id:
        range: quantityValue
      manganate_id:
        range: quantityValue
      copper_id:
        range: quantityValue
      iron_id:
        range: quantityValue
      calcium_id:
        range: quantityValue
      magnesium_id:
        range: quantityValue
      sodium_id:
        range: quantityValue
      potassium_id:
        range: quantityValue
      total_bases_id:
        range: quantityValue
      cation_exchange_capacity_id:
        range: quantityValue
      flag_sulfate:
        range: processeddataflag
      flag_boron:
        range: processeddataflag
      flag_zinc:
        range: processeddataflag
      flag_manganate:
        range: processeddataflag
      flag_copper:
        range: processeddataflag
      flag_iron:
        range: processeddataflag
      flag_calcium:
        range: processeddataflag
      flag_magnesium:
        range: processeddataflag
      flag_sodium:
        range: processeddataflag
      flag_potassium:
        range: processeddataflag
      flag_total_bases:
        range: processeddataflag
      flag_cec:
        range: processeddataflag

  MAOMProduct:
    slots:
      - measure_type
      - rep
    attributes:
      id:
        identifier: true
        range: processedData
      total_organic_carbon_id:
        range: quantityValue
      total_organic_carbon_avg:
        range: double
      total_nitrogen_id:
        range: quantityValue
      total_nitrogen_avg:
        range: double
      flag_toc:
        range: processeddataflag
      flag_tn:
        range: processeddataflag
      flag_toc_avg:
        range: processeddataflag
      flag_tn_avg:
        range: processeddataflag

  MetaGenomicsProduct:
    slots:
    attributes:
      id:
        identifier: true
        range: processedData
      input_to_step:
        range: metagenomicssteps
      output_to_step:
        range: metagenomicssteps
        required: true

  MicrobialBiomassProduct:
    slots:
      - measure_type
      - rep
    attributes:
      id:
        identifier: true
        range: processedData
      mbc_id:
        range: quantityValue
      mbc_avg:
        range: double
      mbn_id:
        range: quantityValue
      mbn_avg:
        range: double
      flag_mbc:
        range: processeddataflag
      flag_mbn:
        range: processeddataflag
      flag_mbc_avg:
        range: processeddataflag
      flag_mbn_avg:
        range: processeddataflag

  NitrogenAnalysisProduct:
    slots:
      - measure_type
      - rep
    attributes:
      id:
        identifier: true
        range: processedData
      no3_n_id:
        range: quantityValue
      no3_n_avg:
        range: double
      nh4_n_id:
        range: quantityValue
      nh4_n_avg:
        range: double
      flag_no3n:
        range: processeddataflag
      flag_nh4n:
        range: processeddataflag
      flag_no3n_avg:
        range: processeddataflag
      flag_nh4n_avg:
        range: processeddataflag

  PhosphorusAnalysisProduct:
    slots:
      - measure_type
      - rep
    attributes:
      id:
        identifier: true
        range: processedData
      extraction_method:
        range: string
      phosphorus_id:
        range: quantityValue
      phosphorus_avg:
        range: double
      flag:
        range: processeddataflag
      flag_avg:
        range: processeddataflag

  RespirationProduct:
    slots:
      - measure_type
    attributes:
      id:
        identifier: true
        range: processedData
      respiration_rate_per_day_id:
        range: quantityValue
      flag:
        range: processeddataflag

  TextureProduct:
    slots:
      - measure_type
    attributes:
      id:
        identifier: true
        range: processedData
      sand_pct_id:
        range: quantityValue
      silt_pct_id:
        range: quantityValue
      clay_pct_id:
        range: quantityValue
      flag:
        range: processeddataflag

  TomographyProduct:
    slots:
      - measure_type
    attributes:
      id:
        identifier: true
        range: processedData
      roi_volume_voxel:
        range: double
      voxel_size:
        range: double
      connected_pores:
        range: double
      pore_diameter_min:
        range: double
      pore_diameter_max:
        range: double
      pore_diameter_mean:
        range: double
      pore_diameter_median:
        range: double
      pore_diameter_variance:
        range: double
      pore_volume_mean:
        range: double
      total_pore_volume:
        range: double
      permeability_x:
        range: double
      flow_rate_x:
        range: double
      tortuosity_x:
        range: double
      permeability_y:
        range: double
      flow_rate_y:
        range: double
      tortuosity_y:
        range: double
      permeability_z:
        range: double
      flow_rate_z:
        range: double
      tortuosity_z:
        range: double
      flag_xct:
        # range: processeddataflag
        range: string

  WEOMProduct:
    slots:
      - measure_type
      - rep
    attributes:
      id:
        identifier: true
        range: processedData
      total_organic_carbon_id:
        range: quantityValue
      total_organic_carbon_avg:
        range: double
      total_nitrogen_id:
        range: quantityValue
      total_nitrogen_avg:
        range: double
      flag_toc:
        range: processeddataflag
      flag_tn:
        range: processeddataflag
      flag_toc_avg:
        range: processeddataflag
      flag_tn_avg:
        range: processeddataflag

  pHProduct:
    slots:
      - measure_type
    attributes:
      id:
        identifier: true
        range: processedData
      ph:
        range: double
      flag:
        range: processeddataflag