id: https://w3id.org/MONet/basalt-schema/administration
name: basalt-schema-administration
title: Basalt Schema - Administration
description: |-
  Administration-related classes for Basalt.
license: MIT
see_also:
  - https://MONet.github.io/basalt-schema
  - https://github.com/pnnl/analysis-api

prefixes:
  basalt_schema: https://w3id.org/MONet/basalt-schema/

default_prefix: basalt_schema
default_range: string

imports:
  - linkml:types

classes:
  Changelog:
    attributes:
      version:
        identifier: true
        range: string
      changelog:
        range: string
        required: true